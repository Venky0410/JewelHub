name: Deploy JewelHub to EKS

on:
  push:
    branches: [ main ]
  workflow_dispatch:

env:
  AWS_REGION: eu-west-1
  EKS_CLUSTER: steadystackai-cluster

jobs:
  deploy:
    name: Build and Deploy
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@v2
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: ${{ env.AWS_REGION }}

    - name: Login to Amazon ECR
      id: login-ecr
      uses: aws-actions/amazon-ecr-login@v1

    # Use git commit hash as image tag
    - name: Set image tag
      run: echo "IMAGE_TAG=${GITHUB_SHA::8}" >> $GITHUB_ENV

    - name: Build and push product service
      env:
        ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
      run: |
        docker build -t $ECR_REGISTRY/jewelhub-product-service:${{ env.IMAGE_TAG }} ./product-service
        docker push $ECR_REGISTRY/jewelhub-product-service:${{ env.IMAGE_TAG }}

    - name: Build and push cart service
      env:
        ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
      run: |
        docker build -t $ECR_REGISTRY/jewelhub-cart-service:${{ env.IMAGE_TAG }} ./cart-service
        docker push $ECR_REGISTRY/jewelhub-cart-service:${{ env.IMAGE_TAG }}

    - name: Build and push order service
      env:
        ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
      run: |
        docker build -t $ECR_REGISTRY/jewelhub-order-service:${{ env.IMAGE_TAG }} ./order-service
        docker push $ECR_REGISTRY/jewelhub-order-service:${{ env.IMAGE_TAG }}

    - name: Build and push user service
      env:
        ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
      run: |
        docker build -t $ECR_REGISTRY/jewelhub-user-service:${{ env.IMAGE_TAG }} ./user-service
        docker push $ECR_REGISTRY/jewelhub-user-service:${{ env.IMAGE_TAG }}

    - name: Build and push notification service
      env:
        ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
      run: |
        docker build -t $ECR_REGISTRY/jewelhub-notification-service:${{ env.IMAGE_TAG }} ./notification-service
        docker push $ECR_REGISTRY/jewelhub-notification-service:${{ env.IMAGE_TAG }}

    - name: Build and push frontend
      env:
        ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
      run: |
        docker build -t $ECR_REGISTRY/jewelhub-frontend:${{ env.IMAGE_TAG }} ./frontend
        docker push $ECR_REGISTRY/jewelhub-frontend:${{ env.IMAGE_TAG }}

    - name: Update kubeconfig
      run: |
        aws eks update-kubeconfig \
          --region ${{ env.AWS_REGION }} \
          --name ${{ env.EKS_CLUSTER }}

    - name: Deploy to EKS
      env:
        ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
      run: |
        find ./k8s -name "*.yaml" | xargs sed -i \
          "s|ACCOUNT_ID|${{ secrets.AWS_ACCOUNT_ID }}|g"
        find ./k8s -name "*.yaml" | xargs sed -i \
          "s|:latest|:${{ env.IMAGE_TAG }}|g"
        kubectl apply -f k8s/namespace.yaml
        kubectl apply -f k8s/product-service/
        kubectl apply -f k8s/cart-service/
        kubectl apply -f k8s/order-service/
        kubectl apply -f k8s/user-service/
        kubectl apply -f k8s/notification-service/
        kubectl apply -f k8s/frontend/

    - name: Verify deployment
      run: |
        kubectl rollout status deployment/frontend -n jewelhub
        kubectl rollout status deployment/product-service -n jewelhub
        echo "✅ JewelHub deployed successfully!"

    - name: Get JewelHub URL
      run: |
        echo "🌍 JewelHub URL:"
        kubectl get svc frontend -n jewelhub \
          -o jsonpath='{.status.loadBalancer.ingress[0].hostname}'