apiVersion: v1
kind: Service
metadata:
  name: product-service
  namespace: jewelhub
  labels:          
    app: frontend
spec:
  selector:
    app: product-service
  ports:
  - protocol: TCP
    port: 5001
    targetPort: 5001
  type: ClusterIP