apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: jewelhub
spec:
  selector:
    app: user-service
  ports:
  - protocol: TCP
    port: 5004
    targetPort: 5004
  type: ClusterIP